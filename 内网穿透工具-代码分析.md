# 内网穿透工具 · 代码分析
> 本文档从架构、协议、核心机制三个层面分析 Python 版（protocol.py / server.py / client.py / gui.py）与 Go 版（go/）实现。  
> 更新日期：2026-08-07
>

---

## 一、总体架构
### 1.1 角色与端口
```plain
┌─────────────┐      ┌──────────────────────────┐      ┌─────────────────┐
│  公网访客     │      │      公网服务器(服务端)      │      │   内网客户端      │
│ 浏览器/curl  │      │                          │      │  Windows/Mac/Linux│
└──────┬──────┘      └────────────┬─────────────┘      └────────┬────────┘
       │                           │                             │
       │   9028(访客端口,按映射)      │                             │
       │◄─────────────────────────►│  handle_visitor             │
       │                           │                             │
       │                   7000(控制通道,长连接)                   │
       │◄───────────────── handle_control ─────────────────────►│ register_client
       │                   7001(数据通道,按请求动态连接)            │
       │◄────────────────── handle_data ───────────────────────►│ register(conn_id)
       │                           │                             │
       │                           │                             │
       │                           └──── 指令流: new_conn/心跳 ────►│
       │                                                          │──► 127.0.0.1:80
```

| 组件   | 文件                                      | 运行位置     | 职责                   |
| ------ | ----------------------------------------- | ------------ | ---------------------- |
| 协议层 | `protocol.py` / `go/protocol/protocol.go` | 两端共用     | 防粘包收发、双向转发   |
| 服务端 | `server.py` / `go/server/main.go`         | 公网服务器   | 三端口监听、调度、路由 |
| 客户端 | `client.py` / `go/client/main.go`         | 内网电脑     | 控制长连接、转发请求   |
| GUI    | `gui.py`                                  | 内网 Windows | 可视化封装客户端       |
| 配置   | `config.json`                             | 服务端目录   | 端口/映射/超时         |


### 1.2 三个通道的分工
+ **控制通道（7000）**：客户端连上来后**长期存活**，只传小 JSON 指令（注册、心跳、new_conn、映射管理），开销极小。
+ **数据通道（7001）**：**每个请求一条新 TCP 连接**，用 `conn_id` 与访客连接配对后做字节搬运。
+ **访客通道（port****_****map 各端口）**：公网访客入口，一个端口对应一个 `handle_visitor` 监听。

把控制与数据分离的好处：控制通道只承担轻量调度，即使数据流量巨大也不会阻塞指令下发；单条连接故障影响面被隔离在单个请求内。

---

## 二、协议设计（防粘包）
### 2.1 消息帧格式
```plain
┌──────────────────────────────────────────────┐
│  4 字节长度头 (大端无符号整数)   │   JSON 消息体  │
│  struct.pack("!I", len(payload))  │  UTF-8 字节   │
└──────────────────────────────────────────────┘
```

TCP 是**字节流协议**，没有消息边界。   4

发送  你好 我是 张三

        你好再见那天的张三去买了粉
    
         你好我是张，三

服务端可能一次收到多条消息粘在一起（粘包），或一条消息分多次到达（半包）。解决方式：发送端先写 4 字节大端长度头，接收端先 `readexactly(4)` 读出长度，再 `readexactly(length)` 精确切出消息体。

```python
# protocol.py 核心
header = await reader.readexactly(HEADER_SIZE)          # 读 4 字节头
(length,) = struct.unpack(HEADER_FMT, header)           # 解出消息体长度
payload = await reader.readexactly(length)              # 精确读消息体
```

### 2.2 指令集（JSON 消息） 8080  ip信息
| 指令              | 方向                    | 载荷                       | 用途                       |
| ----------------- | ----------------------- | -------------------------- | -------------------------- |
| `register_client` | 客户端→服务端           | `client_id`, `local_ports` | 注册身份并认领本地端口     |
| `register_ack`    | 服务端→客户端           | `claimed`, `conflicts`     | 回执认领结果               |
| `mapping_list`    | 服务端→客户端           | `mapping`                  | 下发当前映射表--映射的端口 |
| `new_conn`        | 服务端→客户端           | `conn_id`, `local_port`    | 通知客户端开一个转发       |
| `register`        | 客户端→服务端(数据通道) | `conn_id`                  | 数据通道配对               |
| `ping` / `pong`   | 双向                    | —                          | 心跳保活                   |
| `conn_error`      | 客户端→服务端           | `conn_id`                  | 本地连接失败上报           |
| `set_mapping`     | 客户端→服务端           | `mapping`                  | 动态修改映射表             |
| `mapping_result`  | 服务端→客户端           | `ok`, `msg`                | 映射修改回执               |


> Go 版 `go/protocol/protocol.go` 的 `Message` 结构体字段与上表 JSON 键严格一致，因此 Go 服务端与 Python 客户端可以互相通信（协议互操作）。
>

---

## 三、一次完整请求的时序
![](https://cdn.nlark.com/yuque/__mermaid_v3/01c3d06e8bb14a4de836cdc230d58943.svg)

### 关键机制：`PendingConn` 握手
访客连接先到服务端，但此时客户端的数据通道还没建立。服务端把访客连接挂进 `pending[conn_id]`，等客户端在 7001 注册同一 `conn_id` 后配对：

```plain
访客协程                             数据通道协程
  │ 创建 PendingConn(ready, done)      │
  │ 发 new_conn ────────────────────────► 客户端连本地服务
  │                                     │ 连 7001 register
  │◄──── 配对成功(pe.data_* 赋值) ───────┤
  │ ready.set_result(True) ────────────►│
  │ pipe_both(访客, 数据)                │ await done
  │     │                                │
  │     └── 转发结束: done.set_result ────► 退出
```

`ready`（配对是否成功）和 `done`（转发是否结束）两个 future 协调两个协程：超时未配对则访客连接被关闭；客户端上报 `conn_error` 则 `ready` 置 `False` 提前终止。

---

## 四、核心机制详解
### 4.1 心跳保活与看门狗
```plain
客户端 _heartbeat 协程                  服务端 handle_control
  每 30s:                                每收到任意消息:
    send ping ──────────────────────────►  更新 last_seen=now
    wait_for(pong, 10s)                  回复 pong
  10s 内未收到 pong ──► 判定失效 ──► 关闭连接
                                              │
  服务端看门狗 reap_idle_clients（每10s扫一次）  │
  发现 last_seen 超 120s ──► 强制关闭该客户端连接 │
```

+ 客户端主动探测（pong 超时）与服务器被动巡检（看门狗）双保险，任何一方网络卡死都会被清除。
+ 控制连接断开后，客户端主循环 `run()` 以**指数退避**重连：`1s → 2s → 4s → … → 60s` 封顶，成功后归零。

### 4.2 多客户端端口独占路由（核心新增）
服务端用 `port_owner: {本地端口: client_id}` 登记端口归属：

```python
# server.py 注册段
for p in local_ports:
    if p in self.port_owner and self.port_owner[p] != client_id:
        conflicts.append(p)        # 已被其他客户端认领 -> 拒绝
    else:
        self.port_owner[p] = client_id   # 认领成功
        claimed.append(p)

# server.py 路由
def _pick_client(self, local_port=None):
    owner = self.port_owner.get(local_port)   # 端口有归属 -> 只派给归属客户端
    if owner in self.clients:
        return owner
    return next(iter(self.clients))           # 无归属 -> 退回第一个在线客户端
```

客户端断开时，`finally` 块遍历 `port_owner` 释放该客户端认领的所有端口，其他客户端即可重新认领。

### 4.3 动态映射（set_mapping）
GUI 表格修改后提交 `set_mapping`，服务端 `_apply_mapping` 做 diff：

1. 计算 `to_remove`（服务端有、目标没有的端口）与 `to_add`（目标新增或端口变化的）
2. 先停旧端口：`server.close()` 关闭对应 `asyncio.Server`
3. 再起新端口：`asyncio.start_server` 动态监听
4. 全部成功后广播 `mapping_list` 给所有客户端（各 GUI 表格同步刷新），并回执 `mapping_result`

### 4.4 数据转发（pipe_both）
两个方向的搬运各占一个协程（Go 版是两个 goroutine），`FIRST_COMPLETED` 等待：**任意一个方向结束（EOF/异常）即取消另一个并关闭双端**，保证会话能及时收尾、不泄漏句柄。

### 4.5 错误兜底
+ 访客端口未配置映射 → `404 No mapping for this port`
+ 无在线客户端 → `502 No client online`
+ 客户端连不上本地服务 → `conn_error` 上报，服务端回 `502 Backend connect failed`（避免 curl 报 Empty reply）

---

## 五、文件级分析
### Python 版
### client.py
```python
# -*- coding: utf-8 -*-
"""
client.py —— 最简单的客户端（教学）
====================================
运行：先跑 server.py，再跑本文件。

它做的事：
    1. 连上服务端 7000 端口
    2. 一口气连发 3 条消息 —— 故意"连发"是为了演示防粘包：
       没有长度头的话，服务端收到的是一坨混在一起的字节，没法拆分；
       有了长度头，服务端就能一条一条精确拆出来（去看服务端打印）。
    3. 收 3 条回执并打印
    4. 说一句 bye，断开连接
"""
import asyncio
import protocol  # 导入同目录下的 protocol.py

HOST = "127.0.0.1"
PORT = 7000


async def main():
    print(f"[客户端] 连接 {HOST}:{PORT} ...")
    reader, writer = await asyncio.open_connection(HOST, PORT)

    # 1) 连续快速发 3 条，让它们几乎同时穿过网络，考验服务端的"切包"能力
    for i in range(3):
        await protocol.send_msg(writer, {"type": "hello", "data": f"第{i + 1}条打招呼"})
    print("[客户端] 已连发 3 条消息，等待回执...")

    # 2) 收服务端 3 条回执（每条都是完整拆出来的）
    for _ in range(3):
        reply = await protocol.recv_msg(reader)
        print("[客户端] 收到回执:", reply)

    # 3) 说再见并断开
    await protocol.send_msg(writer, {"type": "bye", "data": "我要下线了"})
    writer.close()
    await writer.wait_closed()
    print("[客户端] 已断开，演示结束")


if __name__ == "__main__":
    asyncio.run(main())

```

server.py

```python
# -*- coding: utf-8 -*-
"""
server.py —— 最简单的服务端（教学）
====================================
运行：先跑本文件，再另开终端跑 client.py。

它做的事：
    1. 监听 127.0.0.1:7000 端口
    2. 每个客户端连上后，循环收消息并打印
    3. 每收到一条普通消息就回一句（回显，方便看效果）
    4. 收到 {"type":"bye"} 就断开这条连接
"""
import asyncio
import protocol  # 导入同目录下的 protocol.py

PORT = 7000


async def handle_conn(reader, writer):
    """处理一个客户端连接。reader=读端，writer=写端。"""
    addr = writer.get_extra_info("peername")   # 拿到对方地址，如 ('127.0.0.1', 54321)
    print(f"[服务端] {addr} 连上来了")

    try:
        while True:
            msg = await protocol.recv_msg(reader)   # 阻塞等一条完整消息
            print("[服务端] 收到消息:", msg)

            # 先说再见就断开，不再回执（避免客户端关了还在回写报错）
            if msg.get("type") == "bye":
                print(f"[服务端] {addr} 说再见，关闭连接")
                break

            reply = {"type": "reply", "data": f"服务端已收到: {msg.get('data')}"}
            await protocol.send_msg(writer, reply)  # 回一条消息
    except asyncio.IncompleteReadError:
        # 客户端没打招呼直接拔线会走到这里，属于正常情况
        print(f"[服务端] {addr} 连接意外断开")
    finally:
        writer.close()
        await writer.wait_closed()


async def main():
    # 注册一个回调：每来一个连接就调用 handle_conn
    server = await asyncio.start_server(handle_conn, "127.0.0.1", PORT)
    print(f"[服务端] 已启动，监听 127.0.0.1:{PORT}，等待客户端...")
    async with server:
        await server.serve_forever()   # 一直服务，不退出


if __name__ == "__main__":
    asyncio.run(main())

```



protocol.py 

```python
# -*- coding: utf-8 -*-
"""
protocol.py —— 协议封装（教学最简版）
====================================
这个文件和真正项目里的 protocol.py 做的是同一件事：解决【粘包问题】。

【为什么要防粘包？】
    TCP 是"字节流"协议，没有消息边界。
    你连着发两条消息，对方可能一次收到两段粘在一起（粘包），
    也可能一条消息被拆成好几次才到（半包）。

【解决办法：加长度头】
    消息格式 = 4字节大端长度 + JSON内容
              struct.pack("!I", 消息体长度)
    接收方流程：
        1) 先读 4 个字节  -> 知道这条消息体有多长
        2) 再精确读那么长  -> 得到完整的一条消息

【本文件只提供两个函数】
    send_msg(writer, 对象)  发送一条 JSON 消息（自动加长度头）
    recv_msg(reader)        接收一条 JSON 消息（按长度头切出来）
"""
import asyncio
import json
import struct

HEADER_FMT = "!I"                        # !I = 大端序 4 字节无符号整数
HEADER_SIZE = struct.calcsize(HEADER_FMT)  # 固定等于 4
MAX_MSG_LEN = 10 *1024 * 1024                # 防御性上限：单条消息最大 1MB


async def send_msg(writer, obj):
    """把 obj（通常是字典）变成 JSON 字节，加上 4 字节长度头后发出去。"""
    payload = json.dumps(obj, ensure_ascii=False).encode("utf-8")  # 字典 -> 字节
    if len(payload) > MAX_MSG_LEN:
        raise ValueError("消息太大！")

    header = struct.pack(HEADER_FMT, len(payload))   # 例如 b'\x00\x00\x00\x0e'
    writer.write(header + payload)                   # 长度头 + 内容，一次性写入
    await writer.drain()      # 缓冲一次性写出去                        # 等待底层缓冲真正发出去


async def recv_msg(reader):
    """先读 4 字节长度头，再精确读取消息体，还原成字典。"""
    header = await reader.readexactly(HEADER_SIZE)   # 连读 4 字节，读不满会抛异常（说明连接断了）
    (length,) = struct.unpack(HEADER_FMT, header)    # 解出这条消息体的长度
    body = await reader.readexactly(length)          # 按长度精确读，不多不少 = 完整一条
    return json.loads(body.decode("utf-8"))          # 字节 -> JSON -> 字典

```

文件	教学要点	对应真项目

protocol.py	只保留 send_msg / recv_msg 两个函数，讲透防粘包原理（先读4字节长度→再精确读体）	protocol.py

server.py	asyncio.start_server 监听，循环收消息→打印→回执，演示服务端如何"切包"	server.py 的控制通道雏形

client.py	open_connection 连接，故意连发3条考验拆包，再收3条回执	client.py 的控制通道雏形

怎么跑（演示完服务端进程已清理，端口已释放）：

终端 A：cd ceshi → python server.py

终端 B：cd ceshi → python client.py